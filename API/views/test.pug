html
  head
    title= title
  body
    h1= message
    h2 Heading 2
    h3 Heading 3
    
    p Normal paragraph text.

    p Some numbers:
    ul
        each val in [1, 2, 3, 4, 5]
            li= val

    p #{lines}:
    ul
        each val in lines
            li= val        

- for (var x = 0; x < 3; x++)
    li item

    -
      var list = ["Uno", "Dos", "Tres",
          "Cuatro", "Cinco", "Seis"]
    each item in list
        li= item